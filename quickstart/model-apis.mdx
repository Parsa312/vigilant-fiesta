---
title: "Use a model API"
description: "Make a request to a model that Lightning AI hosts."
---

Lightning AI hosts models you can call directly with the API. You can develop on Lightning AI from within your own

## Before you begin

Retrieve your API key from the Lightning AI dashboard. To retrieve your key:

1. Select the icon for your account in the top-left corner.
2. Select **Keys**.
3. Select icon next to the key to copy it.

You interact with models using the litai Python SDK.

Use the following command to install it.

```bash
pip install litai
```


After you've installed the library, you can use the following code to make a request.

```python
from litai import LLM
llm = LLM(model="openai/gpt-5", api_key="<LIGHTNING_API_KEY>")
print(llm.chat("Hello, world!"))
```

{/* TODO: verify current model list and endpoint */}

## Next steps

- [Model APIs](/inference/model-apis) — browse available models and see full API reference.
